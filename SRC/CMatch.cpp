#include <cmath>
#include <iostream>

int main(){
    double x = 2;
    double y = 4;
    double z;
    double PI = 3.1416;
    double Celd = 3.99;
    //z = std::min(x,y); --Compara el menor numero
    //z = std::max(x,y); --Compara el numero mayor
    //z= pow(2,4); --Potecia al cuadrado
    //z=sqrt(9); --Raiz Cuadrada
    //z= abs(-3); --Version Positiva de un numero
    //z = round(PI); --Redondea un Numero
    //z = ceil(PI); --Devuelve el número entero más próximo que sea mayor o igual que el valor especificado.
    //z = floor(Celd); -- devuelven un valor de punto flotante que representa el entero más grande que sea menor o igual que 
    
    std::cout<<z<<'\n';
}